{
  "name": "catalogo-v1-firestore-tools",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "seed:firestore": "node scripts/seed-firestore.js",
    "deploy:firestore": "npx firebase deploy --only firestore:rules,firestore:indexes",
    "deploy:functions": "npx firebase deploy --only functions",
    "deploy:hosting": "npx firebase deploy --only hosting",
    "deploy:all": "npx firebase deploy --only functions,hosting,firestore:rules,firestore:indexes",
    "setup:firebase": "node scripts/setup-firebase.js"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0"
  },
  "devDependencies": {
    "firebase-tools": "^14.20.0"
  }
}
